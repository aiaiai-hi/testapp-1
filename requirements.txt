# --- Core ---
# Streamlit 1.36+ нужен для st.dialog (модалки). 
# st.navigation/st.Page доступны с 1.30. Берём актуальную ветку.
streamlit>=1.36,<2.0

# --- Данные ---
pandas>=2.1
numpy>=1.26

# --- Графики (для таба «Метрики») ---
# Streamlit умеет st.line_chart/bar_chart из коробки, но для кастомных
# дашбордов почти всегда нужен plotly или altair. Оставляю plotly —
# он удобнее для интерактивных графиков. Altair идёт в зависимостях streamlit.
plotly>=5.20

# --- БД (опционально, под вашу инфру) ---
# Раскомментируйте то, что используете:
# SQLAlchemy>=2.0          # ORM / connection pool
# psycopg[binary]>=3.1     # PostgreSQL
# pymysql>=1.1             # MySQL
# Для st.connection("sql", ...) хватит SQLAlchemy + драйвера.

# --- Аутентификация (опционально) ---
# В Streamlit 1.42+ есть встроенный st.login через OIDC — внешних либ не нужно.
# Если делаете локальный логин по паролю — пригодится:
# bcrypt>=4.1
# PyJWT>=2.8

# --- Качество жизни ---
# Готовые компоненты (badge, toggle, metric_cards и т.д.):
# streamlit-extras>=0.4
# Красивая таблица с inline-редактированием и групп. колонками:
# streamlit-aggrid>=1.0
